<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="com.example.kodexp.ui.opening.OpeningViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.opening.OpeningFragment">

        <Button
            android:id="@+id/btnGetRandomPokemon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Obtenir un Pokémon aléatoire"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:onClick="@{() -> viewModel.onRandomPokemonButtonClick()}" />

        <ImageView
            android:id="@+id/imgPokemonSprite"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:layout_marginTop="16dp"
            android:visibility="@{viewModel.isImageVisible}"
            app:srcCompat="@{viewModel.pokemonImage}" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
